{
  "id": "evolveclaw-scope",
  "name": "EvolveClaw SCOPE",
  "description": "Self-improving agent prompt evolution — synthesizes, manages, and refines guidelines from execution traces via SCOPE.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "serverUrl": {
        "type": "string",
        "default": "http://127.0.0.1:5757"
      },
      "agentName": {
        "type": "string",
        "default": "openclaw-agent"
      },
      "enabled": {
        "type": "boolean",
        "default": true
      },
      "injectMode": {
        "type": "string",
        "enum": ["append_system", "prepend_context", "both", "auto"],
        "default": "append_system"
      },
      "maxGuidelines": {
        "type": "number",
        "default": 30,
        "minimum": 5,
        "maximum": 200
      },
      "strategicRefreshInterval": {
        "type": "number",
        "default": 10,
        "minimum": 1
      }
    }
  },
  "uiHints": {
    "serverUrl": {
      "label": "SCOPE Server URL",
      "help": "URL of the SCOPE sidecar server (default: http://127.0.0.1:5757)."
    },
    "agentName": {
      "label": "Agent Name",
      "help": "Unique identifier for this agent in SCOPE's memory (default: openclaw-agent)."
    },
    "enabled": {
      "label": "Enable Prompt Evolution",
      "help": "Toggle SCOPE prompt evolution on/off without uninstalling."
    },
    "injectMode": {
      "label": "Injection Mode",
      "help": "Where to inject SCOPE guidelines: append_system (cacheable), prepend_context (per-turn), both, or auto (dynamically switches based on guideline volume)."
    },
    "maxGuidelines": {
      "label": "Max Guidelines",
      "help": "Maximum number of guidelines to keep in memory. Oldest tactical guidelines are evicted first when the cap is reached."
    },
    "strategicRefreshInterval": {
      "label": "Strategic Refresh Interval",
      "help": "Re-fetch strategic rules from the SCOPE server every N steps to pick up newly promoted rules."
    }
  }
}
